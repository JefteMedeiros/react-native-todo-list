import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  datePicker: {
    backgroundColor: '#d10048',
    padding: 15,
    borderRadius: 7,
    textAlign: 'center',
    marginTop: 10,
    color: '#fff',
    fontSize: 17,
    fontWeight: 'bold',
  },
});
